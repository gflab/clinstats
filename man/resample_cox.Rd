% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/survival.R
\name{resample_cox}
\alias{resample_cox}
\title{Resampling-based Cox screening of candidate markers}
\usage{
resample_cox(
  expression,
  time,
  event,
  ratio = 0.8,
  times = 1000,
  p_threshold = 0.05,
  seed = NULL
)
}
\arguments{
\item{expression}{Numeric matrix or data frame with samples in rows and
candidate markers in columns.}

\item{time, event}{Follow-up time and event indicator, one value per sample.}

\item{ratio}{Fraction of samples drawn in each resample.}

\item{times}{Number of resamples.}

\item{p_threshold}{Significance threshold that counts as a selection.}

\item{seed}{Optional integer seed for reproducible resampling.}
}
\value{
A tibble with one row per candidate marker: the number of valid
resamples, the number of resamples in which the marker was significant,
the selection fraction, and the median p-value.
}
\description{
Repeatedly draws subsamples of the samples, fits a univariable Cox model
for every candidate column, and reports how often each candidate reached
\code{p_threshold}. This is the resampling screening previously provided by
\code{calc_resamp_cox()}, reported as per-marker selection frequencies instead
of a raw p-value matrix.
}
\details{
The function uses the \code{future} framework. Set a parallel plan before
calling it to use several workers, for example
\code{future::plan(future::multisession)}.
}
\examples{
set.seed(1)
expr <- matrix(rnorm(60 * 5), nrow = 60, dimnames = list(NULL, paste0("g", 1:5)))
time <- rexp(60)
event <- rbinom(60, 1, 0.7)
resample_cox(expr, time, event, times = 20, seed = 1)
}
