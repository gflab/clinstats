% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/expression.R
\name{clean_expression}
\alias{clean_expression}
\title{Clean an expression matrix and collapse duplicated gene columns}
\usage{
clean_expression(expression, id_from = NULL, id_to = "SYMBOL")
}
\arguments{
\item{expression}{Numeric matrix or data frame with samples in rows and
gene identifiers in columns.}

\item{id_from}{Optional identifier type of the column names, for example
\code{"ENTREZID"}. When supplied, identifiers are translated to \code{id_to} with
\link[org.Hs.eg.db:org.Hs.eg.db]{org.Hs.eg.db::org.Hs.eg.db}, which must be installed.}

\item{id_to}{Target identifier type used when \code{id_from} is supplied.}
}
\value{
A numeric matrix with samples in rows and unique gene identifiers
in columns.
}
\description{
Prepares a gene expression matrix for downstream analysis: drops unnamed
and empty columns, expands columns with \code{" /// "} multi-mappings, maps
gene identifiers when requested, and keeps the column with the largest
median absolute deviation when several columns map to the same gene.
}
\examples{
expr <- matrix(
  c(1, 2, 3, 4, 5, 6, NA, 8),
  nrow = 2,
  dimnames = list(NULL, c("A", "A", "B", ""))
)
clean_expression(expr)
}
