% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/roc.R
\name{roc_summary}
\alias{roc_summary}
\title{Summarise the diagnostic performance of a numeric marker}
\usage{
roc_summary(
  marker,
  outcome,
  positive = NULL,
  direction = "auto",
  best_method = c("youden", "closest.topleft"),
  ci_level = 0.95,
  power = FALSE,
  alternative = c("one.sided", "two.sided")
)
}
\arguments{
\item{marker}{Numeric vector of marker values.}

\item{outcome}{Binary outcome. Logical vectors are coded \code{TRUE} as the
event; numeric 0/1 vectors use 1 as the event. Any other coding requires
\code{positive} to name the event level.}

\item{positive}{Value of \code{outcome} that represents the event.}

\item{direction}{Direction of the comparison passed on to
\code{\link[pROC:roc]{pROC::roc()}}. \code{"auto"} lets \pkg{pROC} choose the direction from the
data; \code{"<"} and \code{">"} fix it explicitly.}

\item{best_method}{Method used to choose the optimal operating point,
\code{"youden"} (default) or \code{"closest.topleft"}.}

\item{ci_level}{Confidence level for the AUC interval.}

\item{power}{If \code{TRUE}, add the power of the AUC test as reported by
\code{\link[pROC:power.roc.test]{pROC::power.roc.test()}}. The value may be \code{NA} when power cannot be
estimated for the sample.}

\item{alternative}{Alternative hypothesis used for the power estimate.}
}
\value{
A tibble with one row: sample size, number of events, AUC and its
confidence interval, the optimal threshold, and the sensitivity,
specificity, positive and negative predictive values, and accuracy at
that threshold.
}
\description{
Computes the area under the receiver operating characteristic curve (AUC)
with a confidence interval and the optimal operating point of a numeric
marker against a binary outcome. A tibble with one row is returned, so the
result can be combined across markers or subgroups.
}
\examples{
set.seed(1)
marker <- c(rnorm(40, 0), rnorm(40, 1))
outcome <- rep(c(0, 1), each = 40)
roc_summary(marker, outcome)
}
\seealso{
\code{\link[=odds_ratio]{odds_ratio()}} for the odds ratio at the optimal threshold.
}
