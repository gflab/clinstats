% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/models.R
\name{cox_table}
\alias{cox_table}
\alias{logistic_table}
\title{Regression tables for clinical variables}
\usage{
cox_table(
  data,
  time,
  event,
  factors = NULL,
  max_time = NULL,
  multivariable = c("all", "significant", "none"),
  significance = 0.05,
  ci_level = 0.95
)

logistic_table(
  data,
  outcome,
  factors = NULL,
  positive = NULL,
  multivariable = c("all", "significant", "none"),
  significance = 0.05,
  ci_level = 0.95
)
}
\arguments{
\item{data}{Data frame holding the variables.}

\item{time, event}{Column names of the follow-up time and event indicator
used by \code{cox_table()}.}

\item{factors}{Character vector of variable names to analyse. Defaults to
every column of \code{data} except the time and event columns.}

\item{max_time}{Optional follow-up limit for \code{cox_table()}. Times beyond
the limit are censored at the limit.}

\item{multivariable}{Which variables enter the multivariable model:
\code{"all"} (default), \code{"significant"}, or \code{"none"}.}

\item{significance}{Significance level used by
\code{multivariable = "significant"}.}

\item{ci_level}{Confidence level of the reported intervals.}

\item{outcome}{Column name of the binary outcome used by
\code{logistic_table()}.}

\item{positive}{Value of \code{outcome} that represents the event.}
}
\value{
A tibble with one row per term and the columns \code{term}, \code{variable},
\code{level}, \code{n}, \code{n_event}, the univariable estimate (\code{hr_univariable} or
\code{or_univariable}) with interval and p-value, and the corresponding
multivariable columns.
}
\description{
\code{cox_table()} and \code{logistic_table()} fit each variable on its own, fit one
multivariable model, and return a single tidy table with the univariable
and multivariable estimates side by side. Numeric variables contribute one
row with a per-unit estimate; factors contribute one row per level against
the reference level.
}
\details{
The multivariable model includes all requested variables by default. With
\code{multivariable = "significant"} only variables whose univariable
likelihood-ratio test is below \code{significance} are included, which
reproduces the screening rule of the original helper functions.
}
\examples{
set.seed(42)
dat <- data.frame(
  time = rexp(60, 0.1),
  event = rbinom(60, 1, 0.7),
  age = rnorm(60, 60, 8),
  stage = factor(sample(c("II", "III"), 60, replace = TRUE))
)
cox_table(dat, time = "time", event = "event")
set.seed(42)
dat <- data.frame(
  response = rbinom(60, 1, 0.4),
  age = rnorm(60, 60, 8),
  sex = factor(sample(c("F", "M"), 60, replace = TRUE))
)
logistic_table(dat, outcome = "response")
}
