% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/models.R
\name{evaluate_model}
\alias{evaluate_model}
\title{Evaluate a prediction model on training and test data}
\usage{
evaluate_model(train, test, formula, positive = NULL, ci_level = 0.95)
}
\arguments{
\item{train, test}{Data frames with the model variables.}

\item{formula}{Model formula, for example \code{outcome ~ marker1 + marker2}.}

\item{positive}{Value of the outcome that represents the event.}

\item{ci_level}{Confidence level of the reported AUC intervals.}
}
\value{
A tibble with one row: sample sizes and event counts of both data
sets, the training and test AUC with confidence intervals, and the
DeLong p-value comparing them.
}
\description{
Fits a logistic regression model on \code{train}, evaluates discrimination on
both \code{train} and \code{test}, and compares the two areas under the curve with
DeLong's test for unpaired samples.
}
\examples{
set.seed(1)
train <- data.frame(y = rbinom(80, 1, 0.5), x = rnorm(80))
test <- data.frame(y = rbinom(40, 1, 0.5), x = rnorm(40))
evaluate_model(train, test, y ~ x)
}
